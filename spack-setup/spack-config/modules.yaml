modules:
  default:
    roots:
      tcl: $SPACK_USER_PREFIX/ext-rocm/modules/tcl
    enable:
      - tcl
    tcl:
      hash_length: 5
      projections:
        all: '{name}/{version}-{compiler.name}'
